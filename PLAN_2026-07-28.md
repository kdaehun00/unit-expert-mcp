# 2026-07-28 스펙업 상세 계획 (unit-expert-mcp)

> 목표: 우리 **클라이언트를 2026-07-28로 스펙업**하기 위한 **테스트 대상 서버**를 만든다.
> 최우선 = 정상(ok) baseline이 2026-07-28로 제대로 통신되는 것. 에러 시나리오는 그 다음.

---

## 0. 현재 상태 (분석 결과)

- **SDK 미사용.** `dependencies = []`, 표준 `http.server`로 MCP를 직접 구현. → wire 동작을 자유롭게 손댈 수 있음.
- 프로토콜 로직은 **`handle_json_rpc()` (server.py:824-879)** 한 곳에 집중.
- 지원 버전: `2024-03-26, 2025-03-26, 2025-06-18, 2025-11-25` (최신 `2025-11-25`).
- 현재는 **stateful**: `initialize`로 세션ID 발급, `GET /mcp`는 `mcp-session-id` 필수(server.py:2850).
- 테스트: `tests/test_minimal_mcp.py` — 대부분 `handle_json_rpc()`를 직접 호출하는 유닛 테스트.

## 1. 2026-07-28 wire 규격 (스펙 확인 완료)

핵심 변경 (2025-11-25 → 2026-07-28):

1. **세션 제거.** `initialize` 핸드셰이크·`Mcp-Session-Id` 없음. GET/DELETE 엔드포인트 제거 → `405 Method Not Allowed`.
2. **per-request 메타데이터.** 모든 요청 `params._meta`에:
   - `io.modelcontextprotocol/protocolVersion` (**필수**, 예 `"2026-07-28"`)
   - `io.modelcontextprotocol/clientCapabilities` (**필수**)
   - `io.modelcontextprotocol/clientInfo` (선택, 권장)
   - 누락 시 → `-32602` (Invalid params) + HTTP 400.
3. **헤더 라우팅 (필수).**
   - `MCP-Protocol-Version` — 모든 POST 필수. body `_meta`의 protocolVersion과 **불일치 시 `-32020` HeaderMismatch + 400**.
   - `Mcp-Method` — 모든 요청 필수 (body `method`와 일치).
   - `Mcp-Name` — `tools/call`/`resources/read`/`prompts/get`에 필수 (body `params.name`/`uri`와 일치).
4. **응답.** JSON-RPC request → `application/json`(단일 객체) 또는 `text/event-stream`(SSE). notification → `202 Accepted` 무body.
5. **result에 `resultType`.** 성공 = `"complete"`, 입력 필요 = `"input_required"`. (구버전은 없음 → 클라가 absent를 complete로 취급)
6. **응답 `_meta`에 `io.modelcontextprotocol/serverInfo`** 권장.
7. 새 에러코드: `-32020` HeaderMismatch, `-32021` MissingRequiredClientCapability, `-32022` UnsupportedProtocolVersion.
8. 버전 미지원 시 → `-32022` + `supported` 목록 + 400.
9. 캐시 힌트(`ttlMs`/`cacheScope`)는 확장 성격 — baseline엔 불필요, 에러 시나리오용으로만 선택.

## 2. 설계 원칙

- **버전 분기.** 요청의 protocol version이 `2026-07-28`이면 신규 경로, 아니면 기존(stateful) 경로 그대로. → 기존 2025 시나리오/테스트 100% 보존(하위호환).
- 버전 판별: `MCP-Protocol-Version` 헤더 우선, 없으면 body `_meta` protocolVersion, 그것도 없으면 legacy(2025-03-26)로 폴백.
- `handle_json_rpc()`가 지금 `(status, headers, payload)`를 반환하는 구조 유지. 헤더 검증은 HTTP 계층(`do_POST`)에서 하되, 버전/‐meta 검증은 `handle_json_rpc` 안에서.

## 3. 변경 대상 (파일: `src/unit_expert_mcp/server.py`)

### 3-1. 상수 (19-34행 근처)
- `SUPPORTED_PROTOCOL_VERSIONS`에 `"2026-07-28"` 추가, `LATEST_PROTOCOL_VERSION = "2026-07-28"`.
- 신규 에러코드 상수: `ERR_HEADER_MISMATCH = -32020`, `ERR_MISSING_CAPABILITY = -32021`, `ERR_UNSUPPORTED_VERSION = -32022`.
- `_meta` 키 상수: `META_PROTOCOL_VERSION`, `META_CLIENT_CAPABILITIES`, `META_CLIENT_INFO`, `META_SERVER_INFO`.

### 3-2. 신규 헬퍼 함수
- `request_protocol_version(headers, payload) -> str` : 버전 판별.
- `is_2026(version) -> bool`.
- `validate_2026_request(headers, payload) -> dict|None` : 헤더 존재/일치, `_meta` 필수 필드, 버전 지원 여부 검사 → 위반 시 (status, error) 반환.
- `json_rpc_result_2026(id, result)` : result에 `resultType: "complete"` + `_meta.serverInfo` 부착.

### 3-3. `handle_json_rpc()` (824-879행)
- 시그니처에 `protocol_version`/`headers` 정보 전달 (또는 config에 실어 전달).
- 2026 요청이면:
  - `initialize`가 와도 처리는 하되 세션ID 헤더 없이 응답(또는 그대로 두고 tools/list 위주). PlayMCP 검증기 흐름 확인 후 결정.
  - `tools/list`/`tools/call` 결과를 `json_rpc_result_2026`로 감싸기.
- 2025 요청이면 기존 로직 그대로.

### 3-4. HTTP 계층 (`do_GET` 2823, `do_POST` 2861)
- `do_POST`: 2026 버전이면 `validate_2026_request` 먼저 → 실패 시 400 + JSON-RPC error. 세션ID 요구 제거.
- `do_GET /mcp`: 2026이면 `405 Method Not Allowed` (GET 스트림 제거). 2025는 기존대로.
- `do_DELETE` 없으면 추가해서 2026은 405.

### 3-5. `/scenario` 페이지 — 상단 버전 토글 + 모드별 재구성 (확정)

**결정: 페이지 최상단에 `[2025] [2026]` 프로토콜 버전 토글을 두고, 토글에 따라 하단 시나리오/미리보기 흐름 전체를 그 버전에 맞게 통째로 바꾼다.** (한 페이지, 모드 분리)

```
┌─ Unit Expert MCP 상태 제어 ──────┐
│  프로토콜 버전:  [ 2025 ] [● 2026 ] │   ← 토글 (config.protocolEra)
├───────────────────────────┤
│  [2026 모드일 때만 보이는 그룹]        │
│  □ _meta.protocolVersion 누락      │
│  □ clientCapabilities 누락         │
│  □ Mcp-Name 헤더·body 불일치        │
│  □ resultType 누락                 │
│  □ 2026인데 세션ID 발급(위반)      │
├───────────────────────────┤
│ 미리보기: POST /mcp tools/list      │
│  (initialize 없이 바로, _meta 포함)  │
└───────────────────────────┘
```

**동작 설계**

1. **config에 `protocolEra` 추가** ("2025" | "2026"). 토글이 이 값을 바꿈. 토큰(`cfg`)에도 실려서 URL로 전달됨.
2. **시나리오 그룹을 버전별로 분리.**
   - 2025 모드 → 기존 `SERVER_ERROR_GROUPS` / `TOOL_ERROR_GROUPS` 그대로.
   - 2026 모드 → 신규 `SERVER_ERROR_GROUPS_2026`:
     - `missing-meta-version` : 요청 `_meta`에 protocolVersion 누락 (서버가 -32602 내야 정상)
     - `missing-client-capabilities` : clientCapabilities 누락 (-32021 MissingRequiredClientCapability)
     - `header-body-mismatch` : Mcp-Name/Method 헤더가 body와 불일치 (-32020 HeaderMismatch)
     - `unsupported-version-2026` : 서버가 안 쓰는 버전 요청 (-32022 + supported 목록)
     - `missing-result-type` : 응답 result에 resultType 누락 (위반 주입)
     - `legacy-session-on-2026` : 2026인데 서버가 Mcp-Session-Id 발급 (위반 주입)
     - tools/list 계열(null/empty/too-many/duplicate)·tool error는 2026에서도 공용 → 재사용
3. **미리보기 터미널(`refreshPreview`, server.py:2718)을 버전 분기.**
   - 2025: 지금처럼 `initialize → tools/list`, 헤더 `MCP-Protocol-Version: 2025-03-26`.
   - 2026: **initialize 생략**, 바로 `tools/list`(필요시 `tools/call`). 요청에:
     - 헤더 `MCP-Protocol-Version: 2026-07-28`, `Mcp-Method: tools/list`
     - body `params._meta = { "io.modelcontextprotocol/protocolVersion": "2026-07-28", "io.modelcontextprotocol/clientCapabilities": {} }`
     - `postMcp()`(server.py:2696)의 하드코딩 헤더를 era 파라미터로 교체.
4. **JS 상태관리**: `collectConfig()`/`setControls()`/`serverErrorsFromConfig()`/`summarize*`를 era 분기. 토글 전환 시 반대편 모드 전용 체크값은 무시(초기화).
5. **표시 URL**: 2026 모드에서 만든 URL을 클라이언트/검증기에 붙이면 서버가 2026 경로로 응답 → 이 페이지가 곧 클라이언트 테스트 콘솔이 됨.

**주의**: HTML/JS가 한 거대한 f-string(server.py:1150-2811). era 분기가 JS에 많이 들어가므로, 렌더 함수를 2025/2026 공용 + 조건부 섹션으로 나눠 관리.

### 3-6. 테스트 (`tests/`)
- 기존 테스트는 2025 기본이라 그대로 통과해야 함(회귀 방지).
- 신규 `test_protocol_2026.py`: 2026 정상 흐름, `_meta` 누락→-32602, 헤더 불일치→-32020, 버전 미지원→-32022.

## 4. 검증 방법 (구현 후)
- 로컬 실행: `PYTHONPATH=src PORT=8000 python -m unit_expert_mcp.server`
- curl로 2026 정상 요청(헤더+`_meta`) → tools/list 성공 확인.
- curl로 `_meta` 누락/헤더 불일치 → 각각 -32602/-32020 확인.
- 기존 pytest 전체 통과 확인 (회귀).

## 5. 미해결 질문 (구현 착수 전 확인 필요)
1. **`initialize`를 2026에서 어떻게?** 스펙상 핸드셰이크가 없지만, PlayMCP 검증기가 여전히 initialize를 호출할 수 있음. → 우리 클라이언트/검증기가 2026에서 실제로 뭘 보내는지 확인 필요.
2. **목표 범위**: (ok baseline만) vs (+2026 에러 시나리오) vs (+2025 유지). 사용자는 "우선 서버부터, ok가 잘 되는지 확인" → **baseline 우선, 에러 시나리오는 후속**으로 이해.
3. 응답을 SSE로도 줄지 — baseline은 `application/json` 단일 응답이면 충분.
