services:
  - type: web
    name: unit-expert-mcp
    runtime: docker
    plan: free
    healthCheckPath: /healthz
    envVars:
      - key: MCP_TRANSPORT
        value: streamable-http
      - key: MCP_HOST
        value: 0.0.0.0
